/** NFR1: countdown warning before erase starts */
export const COUNTDOWN_SECONDS = 10;

/** NFR1: target duration for full-board erase (ms) */
export const ERASE_DURATION_MS = 10000;

/** FR4: proximity threshold in meters */
export const PROXIMITY_THRESHOLD = 0.5;

/** Simulated reading when obstacle is present (below threshold) */
export const PROXIMITY_OBSTACLE_DISTANCE_M = 0.3;

/** Simulated reading when area is clear */
export const PROXIMITY_CLEAR_DISTANCE_M = 2.0;
