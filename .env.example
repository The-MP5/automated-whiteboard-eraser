# Copy to .env and fill in. See README.md — variable names match Supabase dashboard (anon public key).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: legacy alias; same value as VITE_SUPABASE_ANON_KEY if you already use this name locally.
# VITE_SUPABASE_PUBLISHABLE_KEY=
